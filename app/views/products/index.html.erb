<div class="container-fluid">   
	<div class="jumbotron" id="product-jumbotron-header">
	  <h1>Wubba Duckie Products</h1>
	</div>
</div>

<div class="container-fluid">
  <div class="row">
	<%# cache (cache_key_for_products) do %>
    <%= render @products %>
	<%# end %>
  	<% if signed_in? && current_user.admin? %>
    <p><button type="button" class="btn btn-success"><%= link_to 'New Product', new_product_path %></button></p>
    <% end %>
  </div>
</div>


<p id="notice"><%= notice %></p>
   